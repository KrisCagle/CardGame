target_room    = rm_overworld; // child objects override this
door_label     = "Building";   // child objects override this
interact_dist  = 64;
spawn_cooldown = 30; // frames before door can trigger — prevents instant-entry on room load
